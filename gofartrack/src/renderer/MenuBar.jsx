import React from "react";

export const MenuBar = ({ menuOpen, toggleMenu }) => {
  // Extract menu bar component here
  return (
    <div className="bg-white border-b border-gray-200 h-7 flex items-center px-2 text-xs select-none shrink-0">
      {/* Menu items */}
    </div>
  );
};
